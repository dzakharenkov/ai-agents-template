# ── Worktree directories (tracked by git worktree, not by git add) ──
work-architect/
work-developer/
work-documentor/
work-security/
work-tester/

# ── IDE ──
.vscode/
.idea/

# ── Cursor local state (keep .cursor/ tracked — it's the agent config) ──
.cursor/mcp.json

# ── Node ──
node_modules/
package-lock.json
yarn.lock

# ── Python ──
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/
dist/
build/

# ── OS ──
.DS_Store
Thumbs.db

# ── Secrets ──
.env
.env.local
.env.*.local
*.key
*.pem
